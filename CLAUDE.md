# 表達力 3 遍極簡練習 APP

## 最高原則（每次開工前先讀）

### 名詞定義

| 名詞 | 定義 | 不是指 |
| --- | --- | --- |
| 一次練習 | 同一個主題連續講三遍所構成的一筆完整紀錄 | 單獨講的某一遍 |
| 一遍 | 三遍中的其中一次錄音，含錄音檔、逐字稿、數據 | 一次練習 |
| 主題 | 使用者在三遍全部講完之後才補上的標題 | 開始前輸入的東西。本 APP 開始前不輸入任何文字 |
| 逐字稿 | 語音辨識產生的文字，可由使用者事後編輯 | 使用者自己打的講稿 |
| 贅詞 | 系統預設清單內的口頭禪與語助詞 | 使用者自訂的詞（第一版不提供自訂） |
| 數據 | 秒數、字數、語速、贅詞數、贅詞密度 | AI 分析結果 |
| AI 檢查 | 三遍講完後送 Gemini 產生的四項分析 | 評分、等第、改寫後的範例稿 |
| 補字 | 斷網導致逐字稿為空時，使用者事後貼上文字的動作 | 修正辨識錯字（那是編輯） |
| 未完成練習 | 三遍尚未講完就離開的紀錄 | 已作廢的紀錄。未完成練習必須保留並可續接 |

### 各模塊權責

| 模組 | 負責 | 不負責 |
| --- | --- | --- |
| 錄音模組 | 正數計時。**第一版不錄音**（見下方「Android 實測限制」），音檔功能保留程式碼但不啟用 | 語音轉文字、資料保存 |
| 辨識模組 | 即時語音轉文字、斷網偵測與標記 | 錄音、修正錯字、計算數據 |
| 數據模組 | 計算秒數、字數、語速、贅詞數、贅詞密度、與前一遍的差距 | 判斷內容好壞、呼叫 AI |
| 儲存模組 | IndexedDB 讀寫、音檔 7 天清除、釘選、匯出 | 畫面呈現、資料內容的正確性 |
| AI 模組 | 組提示詞、呼叫 Gemini、解析回傳、錯誤處理 | 計算數據、決定何時呼叫 |
| 畫面模組 | 三遍流程引導、逐字稿呈現、歷史紀錄、搜尋、設定 | 任何運算與資料存取邏輯 |

### 技術選型（後續不得擅自更換）

| 項目 | 選用 |
| --- | --- |
| 語言 | HTML + CSS + 原生 JavaScript |
| 框架 | 不使用框架 |
| 資料存哪 | 瀏覽器本機資料庫 IndexedDB |
| 主機部署 | GitHub Pages |
| 外部服務 | 僅 Gemini API（AI 檢查用） |
| AI 模型 | Gemini 3.6 Flash（原訂 2.5 Flash，2026-08-27 實測已不開放新用戶，API 直接指定改用 3.6 Flash） |
| 語音轉文字 | 瀏覽器內建 Web Speech API，語言 zh-TW。**練習中不得同時開啟錄音** |
| 知識怎麼存 | 整包送。每次把三遍逐字稿全部送給 AI，總量約一千餘字，不需要挑段落 |
| 測試工具 | Vitest |
| 跑測試指令 | `npm test` |

變更需先改本節並告知。

### 紅線規則（違反即為做錯，不接受任何理由）

1. **不做通知推播**。任何形式的提醒都不做。
2. **不做連續天數統計、打卡、成就徽章**。
3. **不做註冊、登入、帳號綁定**。
4. **不做雲端上傳**。第一版所有資料只存在本機。
5. **不給分數、等第、排名**。
6. **不給鼓勵性評語**。AI 回傳內容出現讚美或鼓勵即視為錯誤。
7. **AI 不得改寫或代寫講稿**。只能指出問題與引用使用者自己說過的話。
8. **不做動畫特效**。狀態變化用最單純的方式呈現。
9. **不做題庫、主題建議**。
10. **開始練習前不要求輸入任何文字**。

### AI 提示詞的固定約束

送給 Gemini 的提示詞必須包含以下限制，缺一即為錯誤：

- 逐字稿由語音辨識產生，有錯字且無標點，忽略錯字與標點，只評內容與結構
- 不給分數、不給等第、不給鼓勵性評語
- 不改寫、不代寫、不產生範例講稿
- 引用具體建議時，只能引用使用者自己在前幾遍說過的原話
- 回覆總字數上限 500 字

### AI 檢查的四項輸出（固定，不得增減）

1. 該救回的：第 1 或第 2 遍講過但第 3 遍消失的重點，上限 2 項，各附一句理由
2. 該刪但還留著的：第 3 遍中仍屬鋪陳、重複或空話的句子
3. 第 2、3 遍才冒出的新內容：標出來讓使用者自行判斷是好東西還是離題
4. 結論位置：第 3 遍第一句是否為結論，不是的話指出哪一句該提前

### 開發流程：TDD

先寫測試 → 再實作 → 全過才算完成。沒有測試的任務不准標完成。

- 跑測試指令：`npm test`
- 改動既有功能 → 既有測試必須全過
- 純畫面任務至少一條行為測試

### 檢測方式

| 層級 | 怎麼算完成 |
| --- | --- |
| 單一任務 | 該任務的測試全部通過 |
| 整體驗收 | 在 Android Chrome 實機上，能完整跑完一次三遍練習並存檔；關掉再開，資料還在 |
| 改動後回頭檢查 | 動到儲存模組 → 重跑全部測試；動到畫面 → 實機重跑一次完整流程 |

### 已知限制（不是 bug，不要修）

- 斷網時逐字稿會是空的。這是預期行為，由使用者事後手動補字。
- 第一版不錄音，所以沒有音檔可以回放或取出。
- 清除瀏覽器資料會遺失全部紀錄。使用者已知情並接受。

## Android 實測限制（2026-08-27 於 Android Chrome 151 實機驗證）

以下三點是實機診斷的結果，不是推測。改動辨識或錄音之前先讀這一節，
否則會把已經修好的 bug 重新做回去。

1. **麥克風同時只給一個來源。** 一旦 `getUserMedia` 開著（MediaRecorder 在錄），
   `SpeechRecognition` 就完全收不到聲音——不會有 `onsoundstart`，只會得到 `onnomatch`，
   逐字稿全空。調換啟動順序沒有用。
   → 所以第一版選擇「只辨識、不錄音」。逐字稿是數據與 AI 檢查的根，優先給它。

2. **`continuous = true` 沒有作用。** 講完一句就會自己 `onend`，
   不自動重新啟動的話，90 秒的一遍只會錄到開頭一句。
   → `speech.js` 必須在 `onend` 時自動接著開，直到使用者按停止。

3. **同一個結果會重複回傳而且愈來愈長**（「今天天氣」→「今天天氣很好」→⋯），
   中間還夾雜空的確定結果。往後串接會得到重複的字，字數與贅詞數會整個算錯。
   → 依 result index 覆寫，而且空字串不覆寫已有內容。

上述三點在 `tests/speech.test.js` 都有對應測試，不要刪。
